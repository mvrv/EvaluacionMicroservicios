<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Principal</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #e9ecef;
            font-family: 'Arial', sans-serif;
        }
        h1, h2 {
            color: #343a40;
        }
        .btn-custom {
            background-color: #28a745;
            color: white;
            transition: background-color 0.3s;
        }
        .btn-custom:hover {
            background-color: #218838;
        }
        .card {
            border: none;
            border-radius: 15px;
            overflow: hidden;
            transition: transform 0.2s;
        }
        .card:hover {
            transform: scale(1.05);
        }
        .card img {
            height: 250px;
            object-fit: cover;
        }
        .nav-buttons a {
            margin: 0 5px;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <div class="container text-center mt-5">
        <h1 class="display-4">Bienvenido a Principal</h1>
    </div>

    <!-- Formulario de Agregar Nueva Película -->
    <div class="container my-5">
        <h2 class="text-center mb-4">Agregar nueva película</h2>
        <form action="/add-item" method="POST" class="mx-auto" style="max-width: 500px;">
            <div class="form-group mb-3">
                <input type="text" name="title" class="form-control" placeholder="Título de la película" required>
            </div>
            <div class="form-group mb-3">
                <input type="text" name="genero" class="form-control" placeholder="Género" required>
            </div>
            <div class="form-group mb-3">
                <input type="text" name="movieId" class="form-control" placeholder="ID de la película" required>
            </div>
            <div class="form-group mb-3">
                <input type="text" name="year" class="form-control" placeholder="Año de lanzamiento" required>
            </div>
            <div class="form-group mb-3">
                <input type="text" name="caratula" class="form-control" placeholder="URL de la carátula" required>
            </div>
            <button type="submit" class="btn btn-custom btn-block">Agregar</button>
        </form>
    </div>

    <!-- Lista de Películas -->
    <div class="container my-5">
        <h2 class="text-center mb-4">Lista de películas</h2>
        <div class="row">
            <% movies.forEach(movie => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= movie.caratula %>" class="card-img-top" alt="<%= movie.title %> Cover">
                        <div class="card-body">
                            <h5 class="card-title"><%= movie.title %></h5>
                            <p class="card-text">Género: <%= movie.genero %></p>
                            <p class="card-text">Año: <%= movie.year %></p>
                            <div class="d-flex justify-content-between">
                                <form action="/delete-item/<%= movie.id %>" method="POST">
                                    <button type="submit" class="btn btn-danger">Eliminar</button>
                                </form>
                                <a href="/edit-item/<%= movie.id %>" class="btn btn-warning">Editar</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <!-- Botones de Navegación -->
    <div class="container text-center my-4 nav-buttons">
        <a href="/" class="btn btn-secondary">Inicio</a>
        <a href="/auth/login" class="btn btn-secondary">Atrás</a>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>
</html>
