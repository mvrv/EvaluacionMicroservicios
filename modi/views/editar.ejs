<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Elemento</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container my-5">
        <h1 class="text-center" style="color: #b22222;">Editar Elemento</h1>

        <!-- Formulario de Edición -->
        <form action="/pagina/edit-item/<%= item.id %>" method="POST" class="mx-auto" style="max-width: 600px;">
            <div class="form-group">
                <label for="songName">Nombre de la canción</label>
                <input type="text" id="songName" name="songName" value="<%= item.songName %>" class="form-control" required placeholder="Nombre de la canción">
            </div>
            <div class="form-group">
                <label for="artistName">Nombre del artista</label>
                <input type="text" id="artistName" name="artistName" value="<%= item.artistName %>" class="form-control" required placeholder="Nombre del artista">
            </div>
            <div class="form-group">
                <label for="coverUrl">URL de la carátula</label>
                <input type="text" id="coverUrl" name="coverUrl" value="<%= item.coverUrl %>" class="form-control" required placeholder="URL de la carátula">
            </div>
            <div class="d-flex justify-content-between mt-4">
                <button type="submit" class="btn btn-primary" style="background-color: #b22222;">Guardar Cambios</button>
                <a href="/pagina/principal" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
